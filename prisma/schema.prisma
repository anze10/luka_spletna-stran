generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model meteoriti {
  id_meteorit Int      @id @default(autoincrement())
  trajanje    Decimal  @db.Decimal(10, 2)
  cas         DateTime
}
